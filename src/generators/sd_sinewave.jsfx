desc:sd_sinewave

slider1: freq=80<0,440,1>Frequency [Hz]
slider2:-9<-72,0,0.1>Volume [dB]

@slider
vol = 10 ^ (slider2 / 20);

@sample
// t = count variable in s
spl0 = sin(2 * $pi * slider1 * t) * vol;
t += 1 / srate;
spl1 = spl0;
