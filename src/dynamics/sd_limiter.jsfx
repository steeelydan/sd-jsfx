desc:sd_limiter

slider1: 0 <-72, 0, 0.1> Threshold [dB]
slider2: 0 <0, 24 , 0.1> Makeup [dB]

@slider
thresh = 10 ^ (slider1 / 20);
makeup = 10 ^ (slider2 / 20);

@sample
spl0 = min(max(spl0, -thresh), thresh) * makeup;
spl1 = min(abs(spl1), thresh) * sign(spl1) * makeup;
